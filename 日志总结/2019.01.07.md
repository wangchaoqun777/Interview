申诉列表
任务一览申诉按钮
申诉详情   不可操作 （执行组意见，仲裁意见）detail 
在线申诉页 保存提交同在线复核页 多出执行组意见可操作

仲裁列表 
仲裁页面 仲裁按钮 （执行组意见）         detail
仲裁详情 不可操作 （执行组意见，仲裁意见） detail


export defalut class Watcher {
  constructor (vm, expOrFn, cb) {
    this.vm = vm
    this.cd = cd
    this.getter = parsePath(expOrFn)
    this.value = this.get()
  }
}